# The pxxxx driver is a prebuilt shared library (pxxxx/pxxxx.dll on Windows,
# pxxxx/libpxxxx.so on Linux) loaded with ctypes.  There is nothing to compile
# and no serial stack to install, so it has no requirements of its own.

# used by demo code only, you may not require
matplotlib
