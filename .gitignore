
.idea/

# a venv created by the venv module ignores itself, but not every tool that
# builds one writes that file, and this repo is cloned onto benches
.venv/
venv/

__pycache__/

*.pyd

p1150_out.csv

# captures written by the MCP server (large, and specific to one bench)
.p1150_runs/

*.so
# the pxxxx driver ships prebuilt shared libraries for each platform
!pxxxx/libpxxxx.so
